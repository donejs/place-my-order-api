#!/usr/bin/env node
var app = require('../lib');
var server = app.listen(require('../lib/config').port);

console.log('App listening at http://%s:%s', server.address().address,
  server.address().port);
